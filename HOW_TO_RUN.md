# 홍익지기 챗봇 실행 방법

홍익지기 챗봇은 정법 강의를 기반으로 질문에 답변하는 AI 챗봇입니다. 이 문서는 챗봇을 실행하는 방법을 설명합니다.

## 사전 준비사항

1. Python 3.8 이상 설치
2. 필요한 패키지 설치: `pip install -e .`
3. OpenAI API 키 설정

## OpenAI API 키 설정 방법

다음 두 가지 방법 중 하나로 API 키를 설정할 수 있습니다:

1. 환경 변수로 설정:
   ```
   export OPENAI_API_KEY=your_api_key
   ```

2. `.env` 파일 생성하여 설정:
   프로젝트 루트 디렉토리에 `.env` 파일을 만들고 다음 내용을 추가합니다:
   ```
   OPENAI_API_KEY=your_api_key
   ```

## 챗봇 실행하기

### 방법 1: 런처 스크립트 사용 (권장)

다음 명령어로 실행합니다:

```
python run_fixed_app.py
```

이 스크립트는 환경 변수를 자동으로 로드하고 애플리케이션을 실행합니다.

### 방법 2: 직접 실행

직접 Gradio 앱을 실행할 수도 있습니다:

```
python gradio_app.py
```

## 문제 해결

**문제**: "벡터 스토어 로드 실패" 메시지가 나옵니다.

**해결책**: 
- OpenAI API 키가 올바르게 설정되었는지 확인합니다.
- data/vector_store 디렉토리가 존재하는지 확인합니다.
- 그래도 문제가 있다면, 간단 모드가 자동으로 활성화되어 계속 사용할 수 있습니다.

**문제**: Gradio 인터페이스가 로드되지 않습니다.

**해결책**:
- 브라우저 캐시를 지우고 다시 시도합니다.
- 다른 브라우저로 시도합니다.
- 포트가 이미 사용 중인 경우 다음 명령으로 프로세스를 종료합니다:
  ```
  lsof -i :7860  # 포트 확인
  kill -9 <PID>  # 프로세스 종료
  ```

## 사용 방법

1. 웹 브라우저가 자동으로 열리고 Gradio 인터페이스가 표시됩니다.
2. 질문 입력란에 정법에 관한 질문을 입력합니다.
3. 시스템은 관련 문서를 검색하여 답변을 생성합니다.
4. 답변에는 출처와 요약 정보가 포함될 수 있습니다.

예시 질문:
- "영혼과 육신의 관계는?"
- "현대 사회가 무너지는 이유는 무엇인가요?"
- "청년이 사회에서 가져야 할 태도는?"
- "운명은 정해져 있나요?"
- "수행이란 정확히 무엇인가요?"
- "정법은 불교나 유교와 무엇이 다른가요?"